name: GitVibe investigate

on:
  workflow_dispatch:
    inputs:
      issue-number:
        description: Issue number to investigate.
        required: true
        type: string
      config-path:
        description: Path to the GitVibe config.
        required: false
        type: string
        default: .github/git-vibe.yml
      dry-run:
        description: Validate without writing to GitHub.
        required: false
        type: boolean
        default: false

jobs:
  investigate:
    uses: git-vibe/actions/.github/workflows/investigate.yml@main
    with:
      issue-number: ${{ inputs.issue-number }}
      config-path: ${{ inputs.config-path }}
      runner: ubuntu-latest
      timeout_minutes: 60
      max_turns: 90
      dry-run: ${{ inputs.dry-run }}
    secrets:
      GITVIBE_GITHUB_TOKEN: ${{ secrets.GITVIBE_GITHUB_TOKEN }}
      GITVIBE_AI_API_KEY: ${{ secrets.GITVIBE_AI_API_KEY }}
